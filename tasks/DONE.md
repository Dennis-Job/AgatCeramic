# DONE

| ID | Название | Дата | Результат | Проверки |
| --- | --- | --- | --- | --- |
| TASK-001 | Initialize Git repository and project structure | 2026-08-11 | Создан Git-репозиторий с основной веткой `main`; зафиксирована базовая структура и добавлены общие правила игнорирования для секретов, зависимостей и артефактов сборки. | `git status --short --branch`; проверка структуры файлов |
| TASK-002 | Initialize Laravel API-only backend | 2026-08-11 | Инициализирован Laravel 13.24.0 в `backend/`; удалены web/Blade/Vite-заготовки, зарегистрирован пустой API route file. Версионирование API и прикладные endpoints остаются в TASK-012. | `composer validate --strict`; `php artisan route:list`; `php artisan test`; `vendor/bin/pint --test` |
| TASK-003 | Initialize Vue Admin | 2026-08-11 | Инициализирована административная SPA-панель на Vue 3, TypeScript и Vite; подключены Vue Router, Pinia и Tailwind CSS, создан базовый каркас интерфейса в стиле TailAdmin. | `npm.cmd run build --prefix frontend/admin` |
| TASK-004 | Initialize Nuxt Client | 2026-08-12 | Инициализирован клиентский интернет-магазин на Nuxt 4.5.2 и TypeScript с включённым SSR; созданы базовый каркас приложения, макет и нейтральная главная страница. | `npm.cmd audit --omit=dev --audit-level=high` — уязвимости не обнаружены; `npm.cmd exec nuxi typecheck`; `npm.cmd run build --prefix frontend/client` |
| TASK-005 | Configure Docker development environment | 2026-08-12 | Настроено локальное Docker Compose-окружение для Laravel API, Vue Admin, Nuxt Client, PostgreSQL 17 и Redis 7; для зависимостей и данных используются именованные volumes. | `docker compose config --quiet`; проверки готовности PostgreSQL и Redis; HTTP 200 для API `/up`, Admin и Client |
| TASK-006 | Configure environment variables and secrets | 2026-08-12 | Добавлены безопасные шаблоны `.env.example` для Docker Compose, Admin и Client; секреты и реальные `.env` исключены из Git. Пароль PostgreSQL удалён из `compose.yaml` и обязателен в локальном корневом `.env`. | `docker compose --env-file .env.example config --quiet`; `git check-ignore` для реальных `.env`; `php artisan about`; `npm.cmd exec nuxi typecheck`; сборки Admin и Client |
| TASK-007 | Configure CI checks | 2026-08-12 | Настроен GitHub Actions workflow для pull request, push в `main` и ручного запуска: проверки Laravel, Vue Admin, Nuxt Client и Docker Compose выполняются отдельными jobs с минимальным правом `contents: read`. | Локальные проверки Composer, Pint, Laravel tests, `npm ci`, audit, сборки Admin/Client, Nuxt typecheck и Compose config; успешный GitHub Actions run после исправления runtime-директорий Laravel |
| TASK-010 | PostgreSQL connection | 2026-08-12 | PostgreSQL настроен как база данных Laravel по умолчанию; безопасные шаблонные `DB_*` переменные добавлены в backend; CI проверяет миграции на PostgreSQL 17, а unit/feature tests остаются изолированными на SQLite `:memory:`. | `composer validate --strict`; `vendor/bin/pint --test`; `php artisan test` — 2 passed; `docker compose --env-file .env.example config --quiet`; `php artisan migrate:fresh --force` в одноразовом backend-контейнере на PostgreSQL 17 |
| TASK-011 | Redis | 2026-08-12 | Redis 7 подключён к Laravel как cache store по умолчанию через расширение `phpredis`. Docker ожидает успешного healthcheck Redis; драйвер очередей остаётся `database` до TASK-016. | `docker compose --env-file .env.example config --quiet`; сборка Docker; запись и чтение cache через Redis в `php artisan tinker`; `composer test` — 3 passed; `vendor/bin/pint --test`; `composer validate --strict` |
| TASK-012 | API v1 routing | 2026-08-12 | Маршрутизация Laravel организована под `/api/v1`, с отдельными файлами для публичных и административных endpoint'ов и версионной точкой входа `GET /api/v1`. | `php artisan route:list --path=api`; `php artisan test` — 6 passed; `vendor/bin/pint --test`; `composer validate --strict` |
| TASK-013 | API error format | 2026-08-12 | Для `/api/v1` введён единый JSON-конверт ошибок с машинным кодом, безопасным сообщением и деталями валидации; внутренние сообщения исключений не раскрываются. | `php artisan test` — 12 passed; `vendor/bin/pint --test`; `composer validate --strict`; `php artisan route:list --path=api` |
| TASK-014 | API Resources convention | 2026-08-12 | Добавлен базовый `ApiResource` со стабильным конвертом `data`; зафиксированы правила размещения, именования, явного выбора полей и сериализации списков с pagination meta/links. | `php artisan test` — 14 passed; `vendor/bin/pint --test`; `composer validate --strict` |
| TASK-015 | Logging and PII masking | 2026-08-12 | Во все настроенные application log channels добавлен Monolog processor, который маскирует чувствительные поля и PII в свободном тексте до форматирования и записи. | `php artisan test` — 17 passed; `vendor/bin/pint --test`; `composer validate --strict`; проверка `php artisan config:show logging` |
| TASK-016 | Queue and scheduler | 2026-08-12 | Очереди Laravel переведены на Redis с отдельной БД `2`; добавлены независимые Docker-сервисы worker и scheduler. Задачи отправляются только после commit транзакции, окончательно неудачные задачи сохраняются в PostgreSQL. | `php artisan test` — 18 passed; `vendor/bin/pint --test`; `composer validate --strict`; `docker compose --env-file .env.example config --quiet`; проверка Redis-очереди и scheduler в Docker |
| TASK-017 | OpenAPI documentation | 2026-08-12 | Добавлена машиночитаемая OpenAPI 3.1-спецификация для реализованной точки входа `GET /api/v1` и общих схем API-ответов/ошибок. Будущие маршруты не объявляются доступными до их реализации. | `php artisan test` — 19 passed; `vendor/bin/pint --test`; `composer validate --strict`; `php artisan route:list --path=api`; `docker compose --env-file .env.example config --quiet` |
| TASK-020 | Admin users | 2026-08-12 | Для административных учётных записей добавлены статус `active`/`blocked`, поле последнего входа `last_login_at`, enum и factory state; seed-пользователь с известными учётными данными удалён. | `php artisan test` — 21 passed; `vendor/bin/pint --test`; PostgreSQL `php artisan migrate --pretend` в Docker |
| TASK-021 | Authentication | 2026-08-12 | Настроена cookie/session аутентификация Laravel Sanctum для Admin SPA: CSRF cookie, `login`/`me`/`logout`, регенерация сессии, rate limiting и запрет входа/доступа для заблокированных учётных записей. | `php artisan test` — 28 passed; `vendor/bin/pint --test`; `composer validate --strict`; PostgreSQL auth tests и `php artisan migrate --pretend` в Docker; CSRF/CORS smoke-check |
| TASK-022 | Roles | 2026-08-13 | Добавлены роли, связь многие-ко-многим `user_roles`, модель, factory и идемпотентный seed семи базовых административных ролей. HTTP-управление ролями отложено до появления permissions и policies, чтобы исключить повышение привилегий. | `php artisan test` — 30 passed; `vendor/bin/pint --test`; `composer validate --strict`; PostgreSQL `php artisan migrate --pretend` и `RoleTest` в Docker |
| TASK-023 | Permissions | 2026-08-13 | Добавлены granular permissions с кодами `module.action`, связь `role_permissions`, наследование прав через роли и baseline-матрица для семи ролей. Управление назначениями будет открыто только вместе с policies в TASK-024. | `php artisan test` — 32 passed; `vendor/bin/pint --test`; `composer validate --strict`; PostgreSQL `php artisan migrate --pretend` и `PermissionTest` в Docker |
| TASK-024 | Policies | 2026-08-13 | Зарегистрированы Laravel policies для сотрудников, ролей и permissions. Проверяется granular доступ: Analyst не получает access-control прав, Administrator может только просматривать, Super Admin — управлять. | `php artisan test` — 33 passed; `vendor/bin/pint --test`; `composer validate --strict`; tests policies в Docker |
| TASK-025 | Audit log | 2026-08-13 | Добавлен append-only audit trail с actor, кодом действия, полиморфной целевой сущностью, временем и санитизированными metadata. Успешные вход и выход администратора фиксируются по кодам `auth.login` и `auth.logout`; для будущих сервисов есть единый `AuditLogService`. | `AuditLogTest` — 2 passed (9 assertions); `vendor/bin/pint --test`; `composer validate --strict`; PostgreSQL migration проверена в Docker |
| TASK-026 | Employee management API and page | 2026-08-13 | Реализованы защищённые CRUD API сотрудников, поиск и фильтрация, назначение существующих ролей, аудит изменений и страница Admin SPA. Нельзя заблокировать или удалить свою учётную запись и лишить систему последнего активного Super Admin. | `AdminUserManagementTest` — 3 passed (18 assertions); `OpenApiDocumentationTest`; полный `php artisan test`; `vendor/bin/pint --test`; `composer validate --strict`; `npm.cmd run build --prefix frontend/admin` |
| TASK-027 | Roles and permission assignments API and page | 2026-08-13 | Реализованы CRUD API пользовательских ролей, редактор ролей и назначений прав в Admin SPA, аудит изменений и признак системной роли. Системные роли нельзя удалить или переименовать; оператор может назначать только permissions, которыми уже обладает. | `RoleManagementTest` — 3 passed (9 assertions); `OpenApiDocumentationTest`; `vendor/bin/pint --test`; `composer validate --strict`; OpenAPI JSON validation; `npm.cmd run build --prefix frontend/admin` |
| TASK-028 | Permission catalogue API and page | 2026-08-13 | Добавлен read-only каталог прав с названиями, техническими кодами, описаниями и назначенными ролями; в Admin SPA доступна страница поиска и фильтрации по модулю. | `PermissionCatalogueTest`; полный `php artisan test`; Pint; Composer; OpenAPI JSON; сборка Admin |
| TASK-029 | Audit log API and page | 2026-08-13 | Добавлен read-only API журнала аудита с пагинацией и фильтрами, а также страница Admin для поиска и просмотра безопасных деталей событий. | `AuditLogApiTest`; полный `php artisan test`; Pint; Composer; OpenAPI JSON; сборка Admin |
| TASK-029A | Admin password reset API and page | 2026-08-13 | Безопасный сброс пароля администратора: одноразовый токен с ограниченным сроком, ответ без раскрытия существования учётной записи, отзыв сессий, аудит успешного сброса и страницы Admin SPA. | `php artisan test` (55 passed); Pint; Composer; OpenAPI JSON; `npm.cmd run build --prefix frontend/admin` |
| TASK-029B | Audit entity snapshots and retention policy | 2026-08-13 | Для событий аудита сохраняются allowlisted snapshots участников и объектов, включая удалённые учётные записи; ежедневная команда удаляет записи старше пяти лет. | `php artisan test` (61 passed); Pint; Composer; PostgreSQL migration preview; `php artisan schedule:list` |
| TASK-029C | Database-level audit log immutability | 2026-08-13 | PostgreSQL trigger запрещает изменения и прямое удаление audit-записей; retention-команда получает ограниченное transaction-local разрешение только для удаления устаревших записей. | `php artisan test` (62 passed); Pint; Composer; PostgreSQL migration applied; `audit:prune` smoke-check; отдельный PostgreSQL integration-test в CI |
| TASK-030 | Categories | 2026-08-14 | Добавлены базовая сущность категорий, административный CRUD API с авторизацией `catalog.manage`, аудит действий и страница управления в Admin SPA. Поддержка дерева и медиа остаётся в TASK-031 и TASK-096. | `php artisan test` (65 passed); Pint; Composer; PostgreSQL migration preview в Docker; `npm.cmd run build --prefix frontend/admin`; OpenAPI JSON validation |
| TASK-031 | Category tree | 2026-08-14 | Добавлена иерархия категорий с `parent_id`, рекурсивным административным API дерева, признаком доступности родительства, защитой от циклов и визуальным отображением дерева в Admin SPA. | `CategoryManagementTest` (6 passed); Pint; PostgreSQL migration в Docker; OpenAPI JSON validation; `npm.cmd run build --prefix frontend/admin` |
| TASK-032 | Attribute groups | 2026-08-14 | Добавлены группы характеристик: migration, model/factory, защищённый CRUD API, аудит, OpenAPI-контракт и страница Admin SPA. | `php artisan test` (70 passed); Pint; Composer; PostgreSQL migration в Docker; OpenAPI JSON validation; `npm.cmd run build --prefix frontend/admin` |
| TASK-033 | Attributes | 2026-08-14 | Добавлены характеристики, типизированные значения select/multiselect, защищённый CRUD API, аудит и страница Admin SPA. | `AttributeManagementTest` (3 passed); Pint; OpenAPI JSON; сборка Admin SPA |
| TASK-034 | Category attributes | 2026-08-14 | Added ordered category-to-attribute assignments, protected API, audit logging, and an Admin SPA editor. | `php artisan test` (75 passed); Pint; Composer; OpenAPI JSON; PostgreSQL migration preview in Docker; `npm.cmd run build --prefix frontend/admin` |
| TASK-035 | Brands | 2026-08-14 | Added brands with a protected admin CRUD API, `catalog.manage` authorization, audit logging, OpenAPI contract, and an Admin SPA page. The optional ISO 3166-1 alpha-2 country of origin is supported. Brand logo/media and SEO metadata remain in their dedicated later modules. | `php artisan test`; Pint; Composer; OpenAPI JSON validation; PostgreSQL migrations in Docker; `npm.cmd run build --prefix frontend/admin` |
| TASK-036 | Products | 2026-08-14 | Added base product cards with required category, optional brand, protected admin CRUD API, audit logging, OpenAPI contract, and an Admin SPA page. Variants, SKU/prices, attributes, images, and relations remain in TASK-037–040. | `ProductManagementTest` (2 passed, 24 assertions); OpenAPI JSON validation; `npm.cmd run build --prefix frontend/admin` |
| TASK-037 | Product variants | 2026-08-14 | Added product-owned variants with globally unique SKU, prices, stock, publication state, ordering, protected nested CRUD API, ownership scoping, and audit logging. Product attributes, images, and relations remain in TASK-038–040. | `ProductVariantManagementTest` (2 passed, 23 assertions); Pint; admin build |
| TASK-038 | Product attributes | 2026-08-14 | Added typed values for category-assigned product attributes, protected full-set replacement API, required-field and option validation, audit logging, and an Admin SPA page. Images and product relations remain in TASK-039–040. | `ProductAttributeValueManagementTest` (2 passed, 20 assertions); Pint; admin build |
| TASK-039 | Product images | 2026-08-14 | Added validated JPEG/PNG/WebP uploads, generated public storage paths, primary-image and ordering management, audit logging, and an Admin SPA page with previews. Related products remain in TASK-040. | `ProductImageManagementTest` (2 passed, 27 assertions); Pint; admin build |
| TASK-040 | Related products | 2026-08-14 | Added related/recommended product relations with ordering, protected atomic replacement API, duplicate/self/reverse-relation validation, audit logging, and an Admin SPA page. | `ProductRelationManagementTest` (2 passed, 19 assertions); Pint; admin build |
| TASK-041 | Product search/filtering | 2026-08-14 | Added protected product search by name, slug, and variant SKU, plus category, brand, activity, stock, and price-range filters with an Admin SPA search page. | `ProductSearchFilterTest` (2 passed, 15 assertions); Pint; admin build |
| TASK-041A | Complete the Catalog OpenAPI contract and reconcile API documentation | 2026-08-17 | Completed the administrative Catalog contract: typed resource and request schemas, all nested product routes, exact pagination envelopes, documented PUT/PATCH behavior, and reconciled API guidance. | `OpenApiCatalogContractTest` (1 passed, 105 assertions); Pint; OpenAPI JSON validation; route-list reconciliation |
| TASK-041B | Require options when changing an attribute to select/multiselect | 2026-08-17 | Attribute updates now require a non-empty replacement option set whenever the submitted type becomes `select` or `multiselect`; ordinary updates that do not change type remain unaffected. | `AttributeManagementTest`; Pint; OpenAPI JSON validation |
| TASK-041C | Remove product image files when deleting a product | 2026-08-17 | Product deletion now collects image storage references before the database cascade and removes every referenced file after a successful transaction. | `ProductManagementTest`; Pint; OpenAPI JSON validation |
| TASK-041D | Enforce a single primary product image under concurrent uploads | 2026-08-17 | Added a partial unique index, existing-data normalisation, and per-product row locking for image mutations, preventing concurrent operations from leaving multiple primary images. Primary reassignment demotes the old image before promoting its replacement. | `ProductImageManagementTest`; Pint; migrations; OpenAPI JSON validation |
| TASK-041E | Make product deletion and image uploads race-safe, including cleanup of every cascaded storage file | 2026-08-17 | Product deletion now acquires the same per-product lock as image mutations, collects image storage references under that lock, and removes every cascaded image file only after a successful transaction. An upload that loses the race cleans up its prewritten file. | Full backend test suite (96 passed, 593 assertions); Pint; OpenAPI JSON validation; `git diff --check` |
| TASK-041F | Enforce the no-reverse-product-relations invariant under concurrent replacements | 2026-08-17 | Relation replacement now locks the source and every target in a consistent order, revalidates reverse relations under those locks, and the database adds a unique unordered-pair index as a final invariant guard. | Full backend test suite (98 passed, 597 assertions); Pint; migration applied to local PostgreSQL; OpenAPI JSON validation; `git diff --check` |
| TASK-041G | Make category-tree acyclicity and parent eligibility race-safe under concurrent updates | 2026-08-20 | Category creation locks the selected parent before checking eligibility. Category moves lock the current tree in ID order before validating the ancestor path, and disabling a parent checks its children while locked. | Full backend test suite (98 passed, 597 assertions); Pint; OpenAPI JSON validation; `git diff --check` |
| TASK-041H | Add pagination handling to the Admin Catalog UI for all paginated catalog collections and selectors | 2026-08-20 | Catalog services now preserve Laravel pagination metadata. Products, search, brands, attribute groups, and attributes use a shared accessible pager; all paginated data used in Catalog selectors and detail editors is loaded across pages so entities are not silently omitted. | `npm.cmd run build --prefix frontend/admin`; `git diff --check`; independent UI Design Guard review; local Admin SPA route check (authentication required for catalog visual QA) |
| TASK-041I | Complete the human Catalog API guide for category, brand, attribute, and image-management behavior | 2026-08-20 | Expanded the human-readable Catalog guide with endpoint behaviour, pagination, validation constraints, full-replacement semantics, examples, category-tree invariants, and image-primary lifecycle rules. | OpenAPI JSON validation; `git diff --check` |
| TASK-041J | Reconcile or remove unused standalone Catalog Admin views | 2026-08-20 | Removed five unreachable standalone product-management views duplicated by the tabbed `/products` editor. The integrated relation selector now loads the complete product set independently of the paginated table. | `npm.cmd run build --prefix frontend/admin`; removed-view reference scan; `git diff --check`; independent UI Design Guard review |
| TASK-041K | Preserve product and variant attribute-value integrity on category changes | 2026-08-20 | Added a reject-on-conflict policy for product category moves and direct or group-driven category-attribute detachment. Category/product/variant writers share an ordered lock protocol and revalidate assignments inside their transactions; required destination attributes must have product-level values. Required category assignments remain a fill-then-validate workflow. | Full backend test suite (103 passed, 630 assertions); PostgreSQL target suite (17 passed, 138 assertions); Pint; Composer; OpenAPI JSON; `git diff --check`; independent backend review |
| TASK-041L | Make attribute type and option changes safe for existing values | 2026-08-20 | Added reject-on-conflict validation for prospective attribute types and full option replacements across product and variant values. Schema and value writers use ordered locks and repeat semantic validation inside transactions; safe conversions and unused-option removal remain available. Attribute changes and requiredness are audited, referenced attributes cannot be deleted, and Admin omits hidden options for non-choice types. | Full backend test suite (110 passed, 683 assertions); PostgreSQL target suite (30 passed, 330 assertions); Pint; Composer; OpenAPI JSON; Admin build; `git diff --check`; independent backend and UI Design Guard reviews |
| TASK-041M | Complete the attribute model required by the Catalog specification | 2026-08-20 | Completed eight strict attribute types (`string`, `text`, `integer`, `decimal`, `boolean`, `select`, `multiselect`, `date`), product-page visibility, typed backend/Admin validation and editors, and legacy `number` to `decimal` normalization. Colors intentionally use `select`: the option label is the color name and its code may be a HEX value; no separate `color` type exists. | Full backend test suite (130 passed, 718 assertions); targeted attribute/OpenAPI suite (36 passed, 249 assertions); Pint; OpenAPI JSON; Admin production build; local authenticated UI verification; `git diff --check`; independent UI Design Guard PASS |
| TASK-041N | Add catalog identifiers and units required for products and variants | 2026-08-20 | Defined variants as the single sellable entity owning SKU, optional globally unique article number and barcode, and a required controlled sale unit. Added PostgreSQL constraints and legacy `piece` backfill, CRUD validation/resources, identifier search, typed Admin fields and accessible states, OpenAPI/human/database documentation, and regression coverage. | Full backend test suite (131 passed, 746 assertions); PostgreSQL migration and targeted suite (7 passed, 80 assertions); Pint; Composer; OpenAPI JSON/contract tests; Admin production build; `git diff --check`; independent backend analysis and UI Design Guard PASS |
| TASK-041O | Trace deferred Catalog media and SEO requirements to their delivery phases | 2026-08-20 | Assigned category images, brand logos, catalog documents, and OG media to Phase 7 Media Library; assigned the complete SEO inventory to Phase 8 while keeping slugs Catalog-owned. Documented the inactive `image_id`/`logo_id` placeholders, safe future FK reconciliation, separate document attachments, and current API boundaries. | Requirements/task/database/API trace review; OpenAPI JSON validation; `git diff --check`; independent documentation audit PASS |
| TASK-041P | Make Admin Catalog pagination resistant to stale requests and last-page deletions | 2026-08-20 | Added a shared latest-request-wins paginated collection composable for products, brands, attributes, and attribute groups. Pagination is disabled and announced while loading; deletion reloads the previous page when appropriate and clamps to the server-reported last page after concurrent removals. Admin unit tests now run in CI. | Admin unit tests (12 passed); Admin production build; CI YAML validation; `git diff --check`; local route check (authentication required for catalog visual QA); independent UI Design Guard PASS |
| TASK-041Q | Bring Catalog dialogs and image ordering into compliance with the UI Design Review standard | 2026-08-20 | Added a shared accessible dialog with naming, focus trapping/restoration, Escape/backdrop handling, suspended parent-modal semantics, and mutation-safe closing. Migrated all Catalog forms and destructive confirmations, named icon-only actions, and added keyboard image ordering with position/status announcements and meaningful preview fallbacks. | Admin unit tests (12 passed); Admin production build; authenticated `/products`, `/categories`, `/brands`, `/attribute-groups`, `/attributes` QA at 320/640/768/1024/1280 px; focus/scroll/dialog interaction checks; `git diff --check`; independent UI Design Guard PASS |
| TASK-041R | Add automated Admin Catalog component and end-to-end coverage | 2026-08-21 | Added Admin Catalog frontend test coverage with Vitest and Playwright for paginated collections, filters, integrated product tabs, selectors, and loading/error/empty-state regressions across the authenticated catalog flows. | Admin unit tests; Playwright Catalog E2E coverage; Admin production build; `git diff --check` |
| TASK-041S | Add real PostgreSQL concurrency regression tests for Catalog invariants | 2026-08-21 | Added real PostgreSQL concurrency regression coverage for primary-image exclusivity, product deletion versus image uploads, reverse relations, and category-tree mutations using competing transactions and dedicated worker support; CI now runs this suite against PostgreSQL. | `php artisan test tests/Integration/PostgresCatalogConcurrencyTest.php`; CI PostgreSQL Catalog concurrency job; `git diff --check` |
| TASK-041T | Make product image storage cleanup durable and retryable | 2026-08-21 | Product and image deletion now persists per-file cleanup intents transactionally, dispatches idempotent deletion jobs after commit, records attempts/errors/completion, and periodically redispatches eligible work with an operator-visible command. | `StorageCleanupTest`; Catalog product/image feature tests; Pint; migrations |
| TASK-041U | Stabilize shared backend test factories used by the Catalog suite | 2026-08-21 | Isolated generated role names and slugs from seeded system-role identifiers with a UUID-backed test namespace, added seeded-role collision regression coverage, and made CI execute the full backend suite twice consecutively. | Targeted `RoleTest` (3 passed, 10 assertions); two consecutive full backend suites (135 passed, 774 assertions each); Pint; Composer validation; `git diff --check` |
| TASK-042A | Define standalone sellable product model | 2026-08-21 | Product is now the only sellable entity and owns globally unique identifiers, commercial data, attributes, and its independent gallery; the obsolete nested Admin API layer was removed. | Full backend suite (140 passed, 775 assertions); OpenAPI contract tests; Pint; Composer validation |
| TASK-042B | Make category attribute requirements assignment-specific | 2026-08-21 | Requiredness moved to category assignments, inactive drafts may remain incomplete, and activation enforces all required product-owned values; variation axes are restricted to scalar types. | Backend activation/attribute tests; Admin unit tests (16 passed); Admin build |
| TASK-042C | Add product variation groups | 2026-08-21 | Added named groups with unique editable codes, ordered multiple axes, atomic membership replacement, one-group-per-product and category/brand/non-axis/tuple invariants. | Product-group feature and concurrency-oriented locking tests; full backend suite |
| TASK-042D | Rebuild Product Admin API | 2026-08-21 | Product CRUD now owns commercial fields and filters; top-level product-group CRUD and server-side relation candidates were added, and Product resources expose group, axis and thumbnail projections. | OpenAPI route/schema coverage; full backend suite; route scan |
| TASK-042E | Migrate legacy products and variants safely | 2026-08-21 | Added idempotent dry-run/apply/finalize conversion with attribute overlay, deterministic SKU-based slugs, independent gallery copies, conflict reporting and an opt-in guarded finalization migration for legacy tables. | Converter rollback/idempotency/finalization tests; local PostgreSQL dry-run/apply/reconciliation/finalize/table cleanup; full backend suite |
| TASK-042F | Rebuild product creation/editing flow | 2026-08-21 | Replaced nested variants with a five-step standalone SKU editor, independent step saves, review-time activation gate and “Create similar product” without identifiers or photos. | Admin build; unit tests (16 passed); Playwright e2e (19 passed) |
| TASK-042G | Add variation-group management to Admin | 2026-08-21 | Admin can create, edit and remove multi-axis groups using server search, thumbnails, SKU and axis values, with compatibility errors shown before recovery. | Playwright group/editor coverage; responsive checks at 320/640/768/1024/1280 px; independent UI Design Guard PASS |
| TASK-042H | Restore/separate related-product management | 2026-08-21 | Related/recommended products remain separate from variation groups and now use server-side candidates, duplicate/reverse exclusions, success feedback and row-scoped validation errors. | Backend relation regression tests; Playwright relation flow; independent UI Design Guard PASS |
| TASK-042I | Complete contracts/regression coverage/UI review | 2026-08-27 | Reconciled the standalone-product contracts and migration lifecycle, completed backend/Admin regression coverage, and performed an authenticated responsive UX/accessibility audit of the full five-step product editor. Axis values now use human-readable option labels instead of raw stored codes. | Full backend suite (140 passed, 775 assertions); Admin unit tests (16 passed); Playwright e2e (19 passed); Admin production build; authenticated 320/640/768/1024/1280 px screenshot audit; overflow, Escape and focus-restoration checks; independent UI Design Guard PASS |
| TASK-042J | Prevent accidental Admin dialog closing after text-selection drags | 2026-08-27 | Backdrop closing now requires both pointer press and release to occur on the backdrop, so dragging a text selection out of a dialog preserves the open form and its data. Migrated the remaining Roles and Employees overlays to the shared accessible `BaseDialog`. | Admin unit tests (18 passed); Playwright e2e (20 passed), including real drag/backdrop regression; Admin production build; modal implementation scan; `git diff --check`; independent UI Design Guard PASS |
| TASK-042K | Streamline similar-product creation | 2026-08-27 | “Create similar product” clears and validates the new name, generates its slug, preserves copied characteristics through the draft workflow, preselects the source and new SKU for a new variation group, or prepares the new SKU for the source product's existing group. Group membership is still written only through the explicit validated group save. | Admin unit tests (18 passed); Playwright e2e (22 passed), including copy/name/slug, copied characteristics, new-group and existing-group regressions plus 320/640/768/1024/1280 px editor coverage; Admin production build; independent UI Design Guard PASS |
| TASK-042L | Automate product image filenames and Alt text | 2026-08-27 | New product images receive deterministic `<SKU>_<ordinal>.<extension>` storage names and matching automatic Alt text under the existing per-product upload lock. Admin replaces manual Alt entry with an accessible file picker that shows the selected filename, accepted formats, and the 10 MiB limit. | Product image and OpenAPI tests (4 passed, 151 assertions); Pint; Admin unit tests (18 passed); Admin production build; Playwright file-selection/upload and responsive checks at 320/640/768/1024/1280 px (5 passed); authenticated local `/products` visual QA; OpenAPI JSON; `git diff --check`; independent UI Design Guard PASS |
| TASK-042M | Redesign the product review step | 2026-08-27 | Product information now appears in a dedicated summary above the full-width related-products section. Review actions use consistent primary, secondary, destructive, hover, focus, loading, and disabled styles; “Добавить” is a proper secondary button. | Admin unit tests (18 passed); Admin production build; Playwright responsive and vertical-order checks at 320/640/768/1024/1280 px (5 passed); authenticated local `/products` visual QA; `git diff --check`; independent UI Design Guard PASS |
| TASK-042N | Synchronize product thumbnails after gallery changes | 2026-08-28 | Product-list thumbnails now update reactively after the first upload, primary-image reordering, and image deletion for both existing and newly created products, without a page refresh. | Admin unit tests (18 passed); Admin Playwright e2e (24 passed), including existing/new-product thumbnail regressions; Admin production build; `git diff --check`; independent UI Design Guard PASS |
| TASK-042O | Add product sale flag | 2026-08-28 | Added the independent `is_on_sale` product flag with a non-destructive `false` default for existing rows, complete Admin CRUD editing, list/review badges for both states, and updated API/database contracts. Applied the migration to local PostgreSQL without changing the product count (7 before, 7 after; all existing rows defaulted to false). | Full backend suite (140 passed, 786 assertions); Admin unit tests (18 passed); targeted Admin E2E plus Axe and 320/640/768/1024/1280 px checks (7 passed); Admin production build; Pint; Composer; OpenAPI JSON; `git diff --check`; independent UI Design Guard PASS |

После завершения каждой задачи добавлять:
- ID;
- название;
- дату;
- краткий результат;
- тесты/checks;
- важные решения.
