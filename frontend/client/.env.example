# Public URL of the Laravel API for the storefront.
# Only values prefixed with NUXT_PUBLIC_ are available in browser code.
NUXT_PUBLIC_API_BASE=http://localhost:8000/api/v1

# Absolute public origin used for canonical and Open Graph URLs in production.
NUXT_PUBLIC_SITE_URL=
