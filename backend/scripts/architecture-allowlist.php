<?php

declare(strict_types=1);

/**
 * Temporary architecture debt only. Every entry must identify the exact violation and the
 * follow-up task that removes it. Stale entries make the guard fail.
 *
 * @return list<array{path: string, rule: string, line: int, task: string, reason: string}>
 */
return [];
