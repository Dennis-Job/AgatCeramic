; Bulk product-image ZIP imports are limited by application validation to 500 MiB.
; Keep PHP's multipart limits slightly higher to allow form-data overhead.
upload_max_filesize = 500M
post_max_size = 520M
max_input_time = 600
max_execution_time = 600
